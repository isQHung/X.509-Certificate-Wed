# Database connection for migration runner (scripts/migration.sh)
# For Supabase pooler (port 6543 for connection pooling, 5432 for direct):
DATABASE_URL=postgresql://...

# Note: Set DATABASE_URL before running migrations locally:
# export DATABASE_URL='<DATABASE_URL>'
# ./scripts/migration.sh migrate
